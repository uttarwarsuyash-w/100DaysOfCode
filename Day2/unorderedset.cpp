void explainUset(){
    unordered_set<int>st;
    
    //lower_bound and upper_bound are not present in unordered_set
    //rest all are same as set
    //as above it does not store in any particular order it has a better compleixty
    //than set in most cases ,except some when collision happens

}